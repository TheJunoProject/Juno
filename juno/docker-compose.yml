services:
  juno-gateway:
    image: ${JUNO_IMAGE:-juno:local}
    environment:
      HOME: /home/node
      TERM: xterm-256color
      JUNO_GATEWAY_TOKEN: ${JUNO_GATEWAY_TOKEN:-}
      JUNO_ALLOW_INSECURE_PRIVATE_WS: ${JUNO_ALLOW_INSECURE_PRIVATE_WS:-}
      CLAUDE_AI_SESSION_KEY: ${CLAUDE_AI_SESSION_KEY:-}
      CLAUDE_WEB_SESSION_KEY: ${CLAUDE_WEB_SESSION_KEY:-}
      CLAUDE_WEB_COOKIE: ${CLAUDE_WEB_COOKIE:-}
      TZ: ${JUNO_TZ:-UTC}
    volumes:
      - ${JUNO_CONFIG_DIR}:/home/node/.juno
      - ${JUNO_WORKSPACE_DIR}:/home/node/.juno/workspace
      ## Uncomment the lines below to enable sandbox isolation
      ## (agents.defaults.sandbox). Requires Docker CLI in the image
      ## (build with --build-arg JUNO_INSTALL_DOCKER_CLI=1) or use
      ## scripts/docker/setup.sh with JUNO_SANDBOX=1 for automated setup.
      ## Set DOCKER_GID to the host's docker group GID (run: stat -c '%g' /var/run/docker.sock).
      # - /var/run/docker.sock:/var/run/docker.sock
    # group_add:
    #   - "${DOCKER_GID:-999}"
    ports:
      - "${JUNO_GATEWAY_PORT:-18789}:18789"
      - "${JUNO_BRIDGE_PORT:-18790}:18790"
    init: true
    restart: unless-stopped
    command:
      [
        "node",
        "dist/index.js",
        "gateway",
        "--bind",
        "${JUNO_GATEWAY_BIND:-lan}",
        "--port",
        "18789",
      ]
    healthcheck:
      test:
        [
          "CMD",
          "node",
          "-e",
          "fetch('http://127.0.0.1:18789/healthz').then((r)=>process.exit(r.ok?0:1)).catch(()=>process.exit(1))",
        ]
      interval: 30s
      timeout: 5s
      retries: 5
      start_period: 20s

  juno-cli:
    image: ${JUNO_IMAGE:-juno:local}
    network_mode: "service:juno-gateway"
    cap_drop:
      - NET_RAW
      - NET_ADMIN
    security_opt:
      - no-new-privileges:true
    environment:
      HOME: /home/node
      TERM: xterm-256color
      JUNO_GATEWAY_TOKEN: ${JUNO_GATEWAY_TOKEN:-}
      JUNO_ALLOW_INSECURE_PRIVATE_WS: ${JUNO_ALLOW_INSECURE_PRIVATE_WS:-}
      BROWSER: echo
      CLAUDE_AI_SESSION_KEY: ${CLAUDE_AI_SESSION_KEY:-}
      CLAUDE_WEB_SESSION_KEY: ${CLAUDE_WEB_SESSION_KEY:-}
      CLAUDE_WEB_COOKIE: ${CLAUDE_WEB_COOKIE:-}
      TZ: ${JUNO_TZ:-UTC}
    volumes:
      - ${JUNO_CONFIG_DIR}:/home/node/.juno
      - ${JUNO_WORKSPACE_DIR}:/home/node/.juno/workspace
    stdin_open: true
    tty: true
    init: true
    entrypoint: ["node", "dist/index.js"]
    depends_on:
      - juno-gateway
