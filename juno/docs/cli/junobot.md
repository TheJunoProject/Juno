---
summary: "CLI reference for `juno junobot` (legacy alias namespace)"
read_when:
  - You maintain older scripts using `juno junobot ...`
  - You need migration guidance to current commands
title: "junobot"
---

# `juno junobot`

Legacy alias namespace kept for backwards compatibility.

Current supported alias:

- `juno junobot qr` (same behavior as [`juno qr`](/cli/qr))

## Migration

Prefer modern top-level commands directly:

- `juno junobot qr` -> `juno qr`
