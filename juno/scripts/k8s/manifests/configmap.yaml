apiVersion: v1
kind: ConfigMap
metadata:
  name: juno-config
  labels:
    app: juno
data:
  juno.json: |
    {
      "gateway": {
        "mode": "local",
        "bind": "loopback",
        "port": 18789,
        "auth": {
          "mode": "token"
        },
        "controlUi": {
          "enabled": true
        }
      },
      "agents": {
        "defaults": {
          "workspace": "~/.juno/workspace"
        },
        "list": [
          {
            "id": "default",
            "name": "Juno Assistant",
            "workspace": "~/.juno/workspace"
          }
        ]
      },
      "cron": { "enabled": false }
    }
  AGENTS.md: |
    # Juno Assistant

    You are a helpful AI assistant running in Kubernetes.
