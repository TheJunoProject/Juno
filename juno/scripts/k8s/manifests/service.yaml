apiVersion: v1
kind: Service
metadata:
  name: juno
  labels:
    app: juno
spec:
  type: ClusterIP
  selector:
    app: juno
  ports:
    - name: gateway
      port: 18789
      targetPort: 18789
      protocol: TCP
